import { EconomicActivitiesPipe } from './economic-activities.pipe';

describe('EconomicActivitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new EconomicActivitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
