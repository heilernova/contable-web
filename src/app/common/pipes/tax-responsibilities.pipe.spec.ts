import { TaxResponsibilitiesPipe } from './tax-responsibilities.pipe';

describe('TaxResponsibilitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new TaxResponsibilitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
